local NOM = require('nibui.NOM')
local d = NOM.dynamic

return {
    x = d'left', y = d'+' (d'^' 'y', 13),
    w = d'^' 'w', h = d'-' (d'^' 'h', 25),
    background = 0,
    d'=>' 'elements.timeline',
    d'=>' 'elements.score'
}
